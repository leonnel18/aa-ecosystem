# Secrets & credentials
.env
data/token_*.json
tools/credentials.json

# Intermediate workbench
.tmp/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Data files (may contain PII)
data/*.json
data/*.csv
# Exceptions: no PII
!data/email_config.json
# dashboard_data.json is the aggregated, published portal payload — the
# single source of truth committed daily by the pipeline (see CLAUDE.md)
!data/dashboard_data.json

# OS
.DS_Store
Thumbs.db
.worktrees/
.wrangler/

# Node
node_modules/

deploy_portal.log
